[pytest]
testpaths = tests
pythonpath = services/api
norecursedirs = .git .venv venv node_modules dist build __pycache__ apps web infra
filterwarnings =
    ignore::DeprecationWarning
markers =
    unit: fast isolated tests (no real DB or network)
    integration: API + PostgreSQL (requires TEST_DATABASE_URL / local docker DB)
